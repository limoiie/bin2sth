import os, random
import fire
import torch
import numpy as np

from tqdm import tqdm 
from torch.optim import Adam
from torch.utils.data import Dataset, Dataloader


def train(epoch, n_batch, n_emb, n_negs, ss, init_lr, cuda):
    pass


if __name__ == "__main__":
    fire.Fire(train)
